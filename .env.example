"""Example environment variables file."""

# Project settings
PROJECT_NAME=eeg_random_forest

# Data paths (optional, defaults to project structure)
RAW_DATA_PATH=/path/to/raw/data
PROCESSED_DATA_PATH=/path/to/processed/data

# Google Cloud Platform settings (optional, for cloud features)
GCP_PROJECT_ID=your-project-id
GCS_BUCKET_NAME=your-bucket-name
GCP_REGION=us-east1

# Model training settings
N_ESTIMATORS=300
MAX_DEPTH=None
MIN_SAMPLES_LEAF=5